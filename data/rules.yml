version: "3.1"

rules:
  - rule: Handle project search with any slot combination
    steps:
      - intent: ask_project
      - action: action_search_projects

  - rule: General Fallback
    steps:
      - action: action_default_fallback