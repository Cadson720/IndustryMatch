version: "3.1"

rules:
  - rule: Search project
    steps:
      - intent: ask_project
      - action: action_search_projects

  - rule: Fallback prompt after inactivity
    steps:
      - action: action_search_projects
      - action: action_fallback_prompt