{
  "name": "industrymatch-root",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm run build",
    "copy:frontend": "rm -rf backend/dist || rmdir /s /q backend\\dist && cp -r frontend/dist backend/ || xcopy frontend\\dist backend\\dist /e /i",
    "start:backend": "cd backend && npm start",
    "deploy": "npm run build:frontend && npm run copy:frontend && npm run start:backend"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "concurrently": "^8.0.1"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "jwt-decode": "^4.0.0"
  }
}
